#include <iostream>
#include <string>
using namespace std;

int main() {
    string str = "Vive la plateforme !";

    // Calcul de la longueur de la chaîne
    int length = str.length();

    // Affichage du résultat
    cout << "La taille est " << length << endl;

    return 0;
}
